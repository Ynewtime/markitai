# Markitai

本文件是项目给编码 agent 的唯一说明来源。`CLAUDE.md` 只有一行 `@AGENTS.md`，用导入的方式引用本文件，所以只改这里，两边自动保持一致。

## CHANGELOG

- 未发布的变更不记在 `Unreleased`（`未发布`）下，默认记成下一个版本号。以最近一次发布的 `x.y.z` 为基准：
  - 涉及特性升级：`y+1`（`1.1.0` → `1.2.0`）
  - 仅涉及修复：`z+1`（`1.1.0` → `1.1.1`）
  - 特性和修复都有时，也可以 `y+1`、`z+1` 同时进位（`1.1.0` → `1.2.1`）
- 发布前标题不带日期，例如 `## [1.2.0]`。
- `CHANGELOG.md` 和 `CHANGELOG.zh.md` 同步修改，章节保持一致（中文版用 新增 / 变更 / 修复）。
