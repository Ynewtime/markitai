{
  "name": "markitai-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "cp ../CHANGELOG.md ./changelog.md && cp ../CHANGELOG.zh.md ./zh/changelog.md && cp ../scripts/setup.sh ./public/setup.sh && vitepress dev",
    "docs:build": "cp ../CHANGELOG.md ./changelog.md && cp ../CHANGELOG.zh.md ./zh/changelog.md && cp ../scripts/setup.sh ./public/setup.sh && vitepress build",
    "docs:preview": "vitepress preview"
  },
  "packageManager": "pnpm@10.32.1",
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.17"
  }
}
