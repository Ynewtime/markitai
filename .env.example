# Markitai 环境变量配置
# 复制此文件为 .env 并填入你的 API Key

# DeepSeek API (推荐，性价比高)
DEEPSEEK_API_KEY=YOUR_DEEPSEEK_KEY

# Google Gemini API (支持 Vision)
GEMINI_API_KEY=YOUR_GEMINI_KEY

# OpenAI API (可选，支持 Vision)
OPENAI_API_KEY=YOUR_OPENAI_KEY

# Anthropic API (可选)
ANTHROPIC_API_KEY=YOUR_ANTHROPIC_KEY

# LiteLLM Proxy (可选，如果使用代理)
# LITELLM_PROXY_URL=http://localhost:4000

# Agent Browser 配置 (用于 URL 抓取，可选)
# 通常全局安装后无需配置: npm install -g agent-browser && agent-browser install
# 仅当使用 pnpm link 从源码开发时才需要设置:
# AGENT_BROWSER_HOME=/path/to/agent-browser
